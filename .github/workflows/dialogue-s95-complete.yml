name: Dialogue S95 Complete
on:
  push:
    branches: [story/dialogue-audit-season95]
    paths: [scripts/apply-dialogue-s95-complete.mjs]
permissions:
  contents: write
jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: story/dialogue-audit-season95
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply manual Season 95 markers
        run: node scripts/apply-dialogue-s95-complete.mjs
      - name: Rebuild data
        run: npm run prepare:data
      - name: Test
        run: npm test
      - name: Refresh dialogue audit
        run: npm run audit:dialogue
      - name: Commit Season 95 source markers and worklists
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/prose/FINAL_ARC_SEASON095_PROSE_DRAFT*.md docs/dialogue-audit
          git commit -m "Resolve named-cast dialogue across Season 95"
          git push
