name: Petals Monarch Return Final Validation

on:
  push:
    branches:
      - story/petals-monarch-isgard-return

permissions:
  contents: write

jobs:
  refine-and-validate:
    if: ${{ !contains(github.event.head_commit.message, '[petals-return-final]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Apply final return refinement
        run: node scripts/refine-petals-monarch-return.mjs

      - name: Remove one-shot tooling from validation tree
        run: |
          rm scripts/refine-petals-monarch-return.mjs
          rm .github/workflows/petals-monarch-return-refine.yml

      - name: Install dependencies
        run: npm ci

      - name: Prepare generated data
        run: npm run prepare:data

      - name: Regenerate dialogue audit
        run: npm run audit:dialogue

      - name: Run unit tests
        run: npm test

      - name: Run quality checks
        run: npm run quality

      - name: Build
        run: npm run build

      - name: Performance budget
        run: npm run budget:perf

      - name: Single-file build
        run: npm run build:single

      - name: Diff hygiene
        run: git diff --check

      - name: Commit validated refinement
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Complete the Petals Monarch battlefield return [petals-return-final]"
          git push origin HEAD:${{ github.ref_name }}
